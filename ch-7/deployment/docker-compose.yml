services:
  scylla-node1:
    image: scylladb/scylla:2025.3
    container_name: scylla-node1
    security_opt:
      - no-new-privileges:true
    hostname: scylla
    profiles: ["scylla"]
    restart: always
    command: >
      --developer-mode 1
      --smp 1
      --memory 1G
      --overprovisioned 1
      --api-address 0.0.0.0
      --reactor-backend=epoll
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'SELECT cluster_name FROM system.local'"]
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 60s
    volumes:
      - scylla-data1:/var/lib/scylla:rw
    networks:
      - internal-network

  scylla-node2:
    image: scylladb/scylla:2025.3
    container_name: scylla-node2
    security_opt:
      - no-new-privileges:true
    hostname: scylla
    profiles: ["scylla"]
    restart: always
    command: >
      --seeds=scylla-node1
      --developer-mode 1
      --smp 1
      --memory 1G
      --overprovisioned 1
      --api-address 0.0.0.0
      --reactor-backend=epoll
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'SELECT cluster_name FROM system.local'"]
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 60s
    depends_on:
      scylla-node1:
        condition: service_healthy
    volumes:
      - scylla-data2:/var/lib/scylla:rw
    networks:
      - internal-network

  scylla-node3:
    image: scylladb/scylla:2025.3
    container_name: scylla-node3
    security_opt:
      - no-new-privileges:true
    hostname: scylla
    profiles: ["scylla"]
    restart: always
    command: >
      --seeds=scylla-node1
      --developer-mode 1
      --smp 1
      --memory 1G
      --overprovisioned 1
      --api-address 0.0.0.0
      --reactor-backend=epoll
    healthcheck:
      test: ["CMD-SHELL", "cqlsh -e 'SELECT cluster_name FROM system.local'"]
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 60s
    depends_on:
      scylla-node2:
        condition: service_healthy
    volumes:
      - scylla-data3:/var/lib/scylla:rw
    networks:
      - internal-network
  
  redpanda-node1:
    image: redpandadata/redpanda:v23.3.13
    container_name: redpanda-node1
    hostname: redpanda-node1
    security_opt:
      - no-new-privileges:true
    user: "101:101"
    command:
    - redpanda
    - start
    - --node-id=1
    - --smp=1
    - --memory=1G
    - --kafka-addr=internal://0.0.0.0:9092
    - --advertise-kafka-addr=internal://redpanda-node1:9092
    - --rpc-addr=0.0.0.0:33145
    - --advertise-rpc-addr=redpanda-node1:33145
    - --seeds=redpanda-node1:33145
    healthcheck:
      test: ["CMD-SHELL", "rpk topic list --brokers redpanda-node1:9092 >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    volumes:
      - redpanda-data1:/var/lib/redpanda/data:rw
    networks:
      - internal-network
      - monitoring

  redpanda-node2:
    image: redpandadata/redpanda:v23.3.13
    container_name: redpanda-node2
    hostname: redpanda-node2
    security_opt:
      - no-new-privileges:true
    user: "101:101"
    command:
    - redpanda
    - start
    - --node-id=2
    - --smp=1
    - --memory=1G
    - --kafka-addr=internal://0.0.0.0:9092
    - --advertise-kafka-addr=internal://redpanda-node2:9092
    - --rpc-addr=0.0.0.0:33145
    - --advertise-rpc-addr=redpanda-node2:33145
    - --seeds=redpanda-node1:33145
    healthcheck:
      test: ["CMD-SHELL", "rpk topic list --brokers redpanda-node2:9092 >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    volumes:
      - redpanda-data2:/var/lib/redpanda/data:rw
    networks:
      - internal-network
      - monitoring
    depends_on:
      redpanda-node1:
        condition: service_healthy

  redpanda-node3:
    image: redpandadata/redpanda:v23.3.13
    container_name: redpanda-node3
    hostname: redpanda-node3
    security_opt:
      - no-new-privileges:true
    user: "101:101"
    command:
    - redpanda
    - start
    - --node-id=3
    - --smp=1
    - --memory=1G
    - --kafka-addr=internal://0.0.0.0:9092
    - --advertise-kafka-addr=internal://redpanda-node3:9092
    - --rpc-addr=0.0.0.0:33145
    - --advertise-rpc-addr=redpanda-node3:33145
    - --seeds=redpanda-node1:33145
    healthcheck:
      test: ["CMD-SHELL", "rpk topic list --brokers redpanda-node3:9092 >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    volumes:
      - redpanda-data3:/var/lib/redpanda/data:rw
    networks:
      - internal-network
      - monitoring
    depends_on:
      redpanda-node2:
        condition: service_healthy

  redpanda-console:
    image: redpandadata/console:v3.5.3
    ports:
      - "8080:8080"
    environment:
      CONFIG_FILEPATH: "/etc/redpanda/redpanda-console.yml"
    volumes:
      - ../api/proto/models.proto:/etc/protos/models/models.proto:ro
      - ./redpanda-console.yml:/etc/redpanda/redpanda-console.yml:ro
    networks:
      - external-network
      - monitoring
    depends_on:
      redpanda-node3:
        condition: service_healthy
    restart: on-failure
  
  prometheus:
    image: prom/prometheus:v3.10.0
    hostname: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    networks:
      - internal-network
      - monitoring
    depends_on:
      wikistats-producer:
        condition: service_started
      wikistats-consumer:
        condition: service_started

  grafana:
    image: grafana/grafana:12.4.1
    volumes:
      - ./grafana.yml:/etc/grafana/provisioning/datasources/datasource.yaml
      - ./grafana_provider.yml:/etc/grafana/provisioning/dashboards/provider.yaml
      - ./grafana_dashboard.json:/var/lib/grafana/dashboards/wikistats.json
      - grafana-data:/var/lib/grafana:rw
    ports:
      - "3000:3000"
    networks:
      - external-network
      - monitoring

  wikistats-database:
    build:
      context: ..
      dockerfile: build/database.Dockerfile
    container_name: wikistats-database
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    hostname: database
    read_only: true
    restart: on-failure
    networks:
      - internal-network
  
  wikistats-api:
    build:
      context: ..
      dockerfile: build/api.Dockerfile
    container_name: wikistats-api
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    hostname: api
    read_only: true
    restart: on-failure
    networks:
      - internal-network
      - external-network
    ports:
      - "7000:7000"
    depends_on:
      wikistats-database:
        condition: service_started

  wikistats-producer:
    build:
      context: ..
      dockerfile: build/producer.Dockerfile
    container_name: wikistats-producer
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    hostname: producer
    read_only: true
    restart: on-failure
    networks:
      - internal-network
      - external-network
    depends_on:
      redpanda-node3:
        condition: service_healthy
  
  wikistats-consumer:
    build:
      context: ..
      dockerfile: build/consumer.Dockerfile
    container_name: wikistats-consumer
    security_opt:
      - no-new-privileges:true
    user: "1000:1000"
    hostname: consumer
    read_only: true
    restart: on-failure
    networks:
      - internal-network
    depends_on:
      wikistats-database:
        condition: service_started
      redpanda-node3:
        condition: service_healthy

networks:
  external-network:
    driver: bridge
    internal: false
  internal-network:
    driver: bridge
    internal: true
  monitoring:
    driver: bridge
    internal: true

volumes:
  scylla-data1:
  scylla-data2:
  scylla-data3:
  redpanda-data1:
  redpanda-data2:
  redpanda-data3:
  grafana-data: